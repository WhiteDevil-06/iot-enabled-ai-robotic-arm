# Git Rules and Workflow

## Branches
- `feature/*` (e.g., `feature/control`)
- `fix/*` (e.g., `fix/servo`)
- `model/*` (e.g., `model/v1`)

## Workflow
1. Pull latest before starting work.
2. Create a clean branch.
3. Commit cleanly.
4. Create Pull Request (PR) → Merge.
